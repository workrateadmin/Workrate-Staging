WORKRATE — DEBUG LIVE GATEWAY ATTESTATION 400

CURRENT LIVE RESULT:

Published WorkRate app:

Settings → Advanced → Diagnostics → HMRC / MTD

Gateway:
CONNECTED

Gateway readiness:
READY

Environment:
SANDBOX

Public IP:
67.207.68.112

Last validation:
6 Sept 2026, 20:09

Fraud status:
UNAVAILABLE

HMRC connectivity:
UNKNOWN

Dynamic submission:
UNAVAILABLE

Actual failure shown:

"Gateway attestation failed (400)."

IMPORTANT:

This is now a LIVE published-browser failure.

Do NOT contact HMRC production.
Do NOT submit a quarterly update.
Do NOT fabricate browser/network telemetry.
Do NOT weaken attestation validation.
Do NOT log secrets or raw sensitive telemetry.
Do NOT publish automatically.

==================================================
1. TRACE THE EXACT 400
==================================================

Trace the live flow:

Published browser
→ POST /api/finance/hmrc/attestation-grant
→ browser receives opaque grant
→ browser POST https://hmrc-gateway.work-rate.uk/v1/attest
→ gateway returns HTTP 400

Identify the exact gateway validation branch returning 400.

Report the normalized reason WITHOUT exposing:

- HMAC secret
- attestation secret
- HMRC secrets
- raw IP/source port
- NINO
- raw authorization token

==================================================
2. COMPARE CONTRACTS
==================================================

Compare the LIVE frontend attestation request against the deployed gateway contract.

Check exact field names/types for:

- grant/token
- tenant/company identifier
- Clerk user identity
- Clerk session identity
- browser user agent
- timezone
- screen dimensions
- window dimensions
- device/browser UUID
- origin
- timestamp
- request ID/nonce

Confirm the browser request matches the gateway’s expected schema exactly.

==================================================
3. CHECK GRANT BINDING
==================================================

Verify the attestation grant generated by WorkRate is bound to the same:

- tenant
- user
- Clerk session
- browser evidence
- origin

that the browser later sends to the gateway.

Check:

- expiry window
- clock skew
- encoding
- serialization
- signed claim names

Do not loosen tenant/session binding.

==================================================
4. CHECK PRODUCTION ORIGIN
==================================================

Confirm the exact browser Origin received by the gateway.

It must match one of the configured allowed origins.

Current expected production origins include:

https://work-rate-manager.replit.app
https://work-rate.uk
https://www.work-rate.uk

If the actual published origin differs, report it.

Do not wildcard CORS.

==================================================
5. SAFE LIVE ERROR DETAIL
==================================================

The UI currently shows only:

"Gateway attestation failed (400)."

Add a safe normalized attestation error code if one does not already exist.

Examples:

ATTESTATION_GRANT_INVALID
ATTESTATION_GRANT_EXPIRED
ATTESTATION_SESSION_MISMATCH
ATTESTATION_TENANT_MISMATCH
ATTESTATION_BROWSER_EVIDENCE_MISSING
ATTESTATION_BROWSER_EVIDENCE_MISMATCH
ATTESTATION_ORIGIN_NOT_ALLOWED
ATTESTATION_SCHEMA_INVALID

Do NOT expose raw claims or telemetry.

==================================================
6. CHECK CADDY OBSERVED CONNECTION HEADERS
==================================================

Verify Caddy is supplying the gateway’s expected directly-observed connection metadata.

Check the deployed contract for:

- observed remote host
- observed remote source port

Confirm:

- Caddy forwards them over loopback only
- Node gateway recognizes the loopback proxy
- browser cannot spoof them
- expected header names match the deployed Caddyfile

If this is the cause of the 400, fix the Caddy/gateway contract rather than accepting browser-supplied IP/port.

==================================================
7. DO NOT WEAKEN SECURITY
==================================================

Do not solve the 400 by:

- removing session binding
- removing tenant binding
- trusting browser IP
- allowing arbitrary origin
- increasing expiry excessively
- skipping signature validation
- accepting missing required evidence

Fix the actual contract mismatch.

==================================================
8. TEST LOCALLY / CONTRACT TEST
==================================================

Add or update focused tests for the exact failure discovered.

At minimum verify:

- valid production-shaped attestation succeeds
- malformed payload returns safe 400
- expired grant rejected
- wrong session rejected
- wrong tenant rejected
- modified grant rejected
- missing browser evidence rejected
- spoofed remote-IP headers rejected
- allowed origin accepted
- unapproved origin rejected

==================================================
9. LIVE VERIFICATION
==================================================

Once fixed in code:

report whether a new publish is required.

Do NOT pretend the live 400 is resolved from dev tests alone.

After publish, the genuine production browser must be used again.

==================================================
10. COMPLETION REPORT
==================================================

Report:

A. exact 400 cause
B. exact contract mismatch
C. fix made
D. whether Caddy changes are required
E. whether gateway service changes are required
F. tests passed
G. whether WorkRate needs one publish
H. whether DigitalOcean gateway needs restart/redeploy
I. exact next manual action

Do not publish automatically.