# Required secrets (generate distinct high-entropy values; do not commit values)
WORKRATE_GATEWAY_HMAC_SECRET=
GATEWAY_ATTESTATION_SECRET=
HMRC_SANDBOX_CLIENT_ID=
HMRC_SANDBOX_CLIENT_SECRET=
# Deployment/runtime configuration
PORT=8081
GATEWAY_PUBLIC_IP=
GATEWAY_VERSION=
WORKRATE_BROWSER_ORIGINS=
# Set these only after confirming the exact sandbox API/version approved for WorkRate.
HMRC_SANDBOX_QUARTERLY_PATH_TEMPLATE=
HMRC_SANDBOX_QUARTERLY_METHOD=
HMRC_SANDBOX_ACCEPT=
# Comma separated, only after written HMRC approval: client-public-port,client-multi-factor,vendor-license-ids
HMRC_FRAUD_APPROVED_OMISSIONS=